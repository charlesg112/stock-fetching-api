describe("Memory Stock Repository", () => {
    const repository = new MemoryStockRepository();

    test("Given saved stocks When finding all stocks Then all stocks are returned", () => {

    })
})
